/**
 * @deprecated Types have been moved to entities.ts. Use imports from there instead.
 */

import {
	TaskId,
	TimeEntryId,
	ProjectId,
	ItemId,
	BaseItem,
	TaskSchema,
	TimeEntrySchema,
	ProjectSchema
} from './entities';

// Re-export types for backward compatibility
export {
	TaskId,
	TimeEntryId,
	ProjectId,
	ItemId,
	BaseItem,
	TaskSchema,
	TimeEntrySchema,
	ProjectSchema
};
